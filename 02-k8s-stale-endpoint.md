# 쿠버네티스 Stale Endpoint로 인한 CrashLoopBackOff 및 서비스 다운

## 1. 개요

- **발생 기간:** 2026-07-31 (금) ~ 2026-08-03 (월)
- **영향 범위:** 공공기관 내부망 Kubernetes 클러스터 (개발 환경) 내 통계 서비스
- **발단:** [01. Startup Probe 503 트러블슈팅](./01-k8s-startup-probe-503.md)을 해결하며 통계 서비스 파드를 재배포한 직후부터 시작됨
- **증상:**
    1. 금요일 재배포 후 API는 `200 OK` 정상 응답. 그런데 `replicas: 1` 설정인데 파드가 2개 떠 있는 것을 목격
    2. "어차피 레플리카가 1이니까 하나는 저절로 사라지겠지" 하고 API 테스트만 확인 후 종료 → **이 판단이 월요일 문제의 시작이 됨**
    3. 월요일 확인 시 파드 2개 중 하나는 `Running`(200 처리 중), 하나는 죽어있는 상태 (당시 정확한 상태값은 기록해두지 않아 불명)

## 2. 복구 시도 과정 (실패 과정 포함)

### ① 1차 시도: 죽은 파드 재시작 (실패, CrashLoopBackOff 시작)

- 죽어있는 구 파드를 재시작함.
- **결과:** `CrashLoopBackOff` ↔ `Running` 상태가 무한 반복되기 시작. 이때 확인한 에러 로그는 `Connection closed` (Spring 부팅 시 Config Server 통신 실패). 처음 만나는 에러였고, Spring Boot가 시작조차 못 하고 죽는 상황이었음.

### ② 2차 시도: readinessProbe 대기 시간 연장 (실패)

01번에서 startupProbe로 해결한 경험이 있어, 이번에도 Probe 문제를 의심하고 readinessProbe를 수정함.

```yaml
readinessProbe:
  httpGet:
    path: /actuator/health
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 10   # 기존 3 -> 10으로 변경 (최대 100초 대기)
```

- **결과:** 대기 시간을 늘려도 동일하게 `CrashLoopBackOff` 반복. **부팅 시간 부족 문제가 아니었음.**

### ③ 3차 시도: 파드 직접 삭제 (실패)

- 실패를 반복하는 파드를 직접 삭제함.
- **결과:** K8s가 `replicas`를 맞추기 위해 파드를 자동 재생성했으나, 새 파드 역시 `Connection closed`로 부팅 실패. 삭제 ➔ 재생성 ➔ 실패가 무한 반복됨.

### ④ 4차 시도: replicas 0 ➔ 1 조작 (실패, 서비스 Down)

- 파드를 전부 정리하고 새로 띄우기 위해 `replicas`를 0으로 내렸다가 1로 올림.
- **결과:** 유일하게 200을 처리하던 정상 파드까지 함께 삭제됨. 새로 뜬 파드는 여전히 부팅에 실패하면서 **서비스가 완전히 Down되고 503이 표출됨.**

### ⑤ 5차 시도: Config Server 및 App Deployment 삭제 후 재배포 (성공)

- DB 커넥션 문제도 의심했으나, 터미널에서 직접 접속 테스트가 불가한 환경이라 로그 기반으로 추론함. Spring Boot가 기동 초기 단계(Config 조회 시점)에 죽는 것으로 보아, 그보다 뒤 단계인 DB 커넥션 풀 생성 문제는 아니라고 판단하고 배제함.
- 최종적으로 Deployment 자체를 삭제하고 CI/CD 파이프라인으로 재배포함.
- **결과:** 정상 부팅 및 `200 OK` 복구.

## 3. 근본 원인 (Root Cause)

> **"K8s 내부 네트워크 주소록(Service Endpoint / CoreDNS)의 Stale IP 갱신 지연"**

코드나 설정 파일(`yml`) 오류가 아닌, **K8s 인프라 단의 L4/L7 라우팅 동기화 및 파드 생명주기(Lifecycle) 이슈**였습니다.

1. **Config Server 파드 재배포 발생:** Config Server가 재배포되며 파드의 실제 IP가 변경됨 (`10.244.1.10` ➔ `10.244.2.25`).
2. **K8s Service 주소록 동기화 누락/지연:** K8s 내부 네트워크(`kube-proxy`, `CoreDNS`, `EndpointSlice`)가 꼬여 이미 삭제된 옛날 파드 IP를 주소록에 그대로 유지함.
3. **신규 부팅 파드 접속 실패:** 새로 생성되는 파드가 Service 도메인(`config-service.<namespace>.svc.cluster.local`)으로 접속을 시도했으나, K8s 주소록이 죽어있는 옛날 IP로 길을 안내함.
4. **결과:** 네트워크 단에서 즉시 TCP RST (`Connection closed`) 응답이 반환되며 Spring Context 초기화 실패로 파드가 튕김.

### 주소록이 갱신되지 않았던 이유 (내부 메커니즘)

파드 IP가 바뀌는 속도를 K8s 내부의 동기화 컴포넌트들이 따라가지 못한 것으로, 대표적인 원인은 다음과 같습니다.

1. **CoreDNS 캐싱 지연:** 파드 IP가 바뀌었지만 CoreDNS 내부에 기존 IP 캐시가 만료되지 않고 남아있는 현상.
2. **kube-proxy 라우팅 테이블 동기화 누락:** IP 변경 이벤트가 노드의 네트워크 규칙(iptables/IPVS)에 제때 반영되지 못하는 현상.
3. **EndpointSlice 동기화 딜레이:** 주소록 교체 타이밍이 어긋나 옛날 IP가 미처 삭제되지 않고 남는 현상.

### 💡 용어 쉽게 이해하기

- **Config Server (파드):** 애플리케이션에 설정값을 전해주는 **114 안내원**
- **K8s Service:** 안내원에게 연결시켜 주는 **전화번호부 (주소록)**
- **장애 상황:** 안내원 번호가 바뀌었는데, 전화번호부가 옛날 번호를 안내해서 통화가 즉시 끊긴 상태.

## 4. 주요 연관 현상 분석 (타임라인별 복기)

### Q1. 금요일 배포 직후 200 OK를 준 파드는 어느 쪽이었을까?

- startupProbe를 수정해 재배포한 신규 파드(B)가 실제로 정상 부팅에 성공해 200을 처리하고 있었음. 관리 콘솔에서 Running 파드에 변경된 yml이 적용된 것을 확인함.
- 문제는 `RollingUpdate` 과정에서 삭제됐어야 할 구 파드(A)가 완전히 종료되지 못하고 죽은 상태로 남아있던 것.
- 즉, 배포 자체는 성공이었고, 진짜 이상 신호는 **"지워졌어야 할 구 파드가 지워지지 않고 있다"** 는 사실이었음.

### Q2. 구 파드를 재시작하자 왜 CrashLoopBackOff가 시작됐을까?

- 이 시점에 Config Server 재배포로 파드 IP가 변경되어 있었고, K8s Service 주소록에는 옛 IP가 남아있었음(Stale Endpoint).
- 재시작된 구 파드는 부팅 시 Config Server를 찾아가야 하는데, 주소록이 죽은 IP를 안내하여 `Connection closed`로 부팅에 실패. 재시작마다 같은 실패를 반복하며 `CrashLoopBackOff`에 빠진 것으로 추정됨.
- 반면 신규 파드(B)는 주소록이 꼬이기 전 정상적으로 커넥션을 맺어둔 상태라 영향을 받지 않고 200을 유지함.

### Q3. replicas를 0으로 내렸다가 1로 올렸을 때 왜 전체 Down이 됐을까?

- 200을 응답해주던 유일한 정상 파드(B)는 주소록이 꼬이기 이전에 커넥션을 맺어둔, **같은 방법으로 다시 만들 수 없는 상태의 파드**였음.
- `replicas: 0` 조작으로 이 유일한 정상 파드마저 완전히 삭제해 버림.
- 이후 새로 생성되는 파드는 부팅 시점에 꼬여있는 Stale IP를 참조하므로 전부 `Connection closed`로 실패했고, 서비스 셧다운으로 확산됨.

### Q4. Deployment 삭제 후 재배포는 왜 해결됐을까?

- 파드만 껐다 켜는 레벨에서는 Stale 엔드포인트 정보가 남아있을 수 있음.
- Deployment 자체를 삭제하면 K8s가 해당 리소스에 얽힌 Service Endpoint 매핑을 **완전히 삭제(Clear)** 하고, 재배포 과정에서 새로 뜬 파드의 실제 IP 기반으로 주소록을 **신규 등록(Re-register)** 함.
- 주소록 자체가 새것으로 교체되었기 때문에 정상적인 최신 Config Server IP로 길 안내를 받고 통신에 성공하여 정상 동작 확인.

## 5. 재발 방지: 배포 전략 변경 `RollingUpdate` ➔ `Recreate`

```yaml
spec:
  replicas: 1
  strategy:
    type: Recreate  # RollingUpdate 대신 적용
```

- `replicas: 1` 단일 파드 환경에서 `RollingUpdate`를 쓰면 구/신규 파드가 순간 동시에 뜨며 자원 해제 지연, IP 갱신 꼬임, DB/세션 충돌 위험이 상승함.
- `Recreate` 전략은 **기존 파드 및 엔드포인트를 완전히 제거한 후 새 파드를 생성**하므로, Stale IP 패킷 전달 및 라우팅 꼬임을 근본적으로 차단함.

## 6. 배운점

- **이상 신호를 방치하지 않는다:** `replicas: 1`인데 파드가 2개 떠 있는 것을 보고도 "저절로 사라지겠지" 하고 넘어간 것이 이번 장애의 시작이었다. 겉보기 정상(200 OK)과 실제 정상은 다르다.
- **복구 조치 전에 현재 무엇이 서비스를 지탱하는지 파악한다:** replicas 조작으로 유일한 정상 파드를 스스로 삭제한 것이 가장 뼈아픈 실수였다.
- **파드 단위 조치가 안 통하면 상위 리소스를 의심한다:** 파드를 갈아치워도 반복되는 실패는 파드가 아니라 Service/Endpoint 등 공유 인프라 문제일 수 있다.
- **배포 전략은 replicas 수와 함께 결정한다:** 단일 replica 환경에 RollingUpdate는 조합 자체가 위험 요인이다.
- **장애 당시 상태를 기록으로 남긴다:** 죽어있던 구 파드의 정확한 상태값을 기록해두지 않아 복기 과정에서 추정에 의존해야 했다. 조치 전에 상태와 로그를 먼저 남기는 습관이 필요하다.