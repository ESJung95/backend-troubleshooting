# 프론트-백엔드 간 검증 로직 중복과 정합성 불일치 해결을 위한 SSOT 설계

## 1. 개요

- **발생 기간:** 2026-01-30 ~ 2026-02-04
- **영향 범위:** Frontend (Next.js) ↔ Backend (Spring Boot) 간 비즈니스 수식 연산 및 저장 파이프라인
- **증상:**
    1. 실시간 입력 피드백(UX)을 위해 프론트(JS)에 작성한 계산식과 무결성 검증을 위해 백엔드(Java)에 작성한 계산식이 **이중 관리(Duplication)** 됨.
    2. 산식 수정 시 한쪽 코드의 반영이 누락되거나 소수점/반올림 처리 방식 차이로 인해 **화면의 합계와 DB 저장/검증 결과가 어긋나는 정합성 깨짐** 발생.
    3. 동일한 통계 산식 및 검증 로직이 JavaScript와 Java로 이중 구현되어 있어 유지보수 비용과 인적 오류 위험이 증가함.

## 2. 문제 발생 및 접근 과정 (실패와 고민의 과정)

### ① 1단계: 프론트/백엔드 이중 하드코딩의 한계 직면

- **상황:**
    - 화면에서 사용자가 지표(처리구역 인구, 하수관로 연장 등)를 입력할 때마다 실시간 합계(`popltnSm` 등)를 즉각 보여주기 위해 Next.js 폼 핸들러 내에 JavaScript로 계산 함수를 작성함.
    - 동시에 서버 검증(Submit) 시점의 위변조 방지를 위해 Spring Boot Service 계층에도 동일한 Java 계산 로직을 중복 구현함.
- **문제 발생:**
    - 통계 지표 산식 정책이 변경(예: 외국인 인구 제외 → 행정구역 면적 합계 기준 검증으로 변경 등)될 때마다 두 저장소의 코드를 각각 수정해야 함.
    - 프론트엔드 배포와 백엔드 배포 시점의 차이로 인해 **"화면에서는 정상 통과로 뜨는데, 저장 버튼을 누르면 서버 검증에서 400 Validation Error를 뱉는 현상"**이 반복됨.
      
```json
{
  "status": 400,
  "error": "BAD_REQUEST",
  "message": "입력된 합계(popltnSm: 1250)와 서버 산출 검증값(1249)이 일치하지 않습니다."
}
```

### ② 2단계: 대안 3가지 아키텍처 패턴 비교 분석

비즈니스 계산식과 검증 규칙의 **단일 진실 공급원(Single Source of Truth, SSOT)**을 확보하기 위해 세 가지 접근 방식을 검토함.

| 비교 항목 | 방안 1. "Formula as Data" (JSON Logic) | 방안 2. "API Driven Calculation" (실시간 서버 연산) | 방안 3. "Polyglot" (GraalVM) |
|---|---|---|---|
| **동작 방식** | 수식을 **JSON 규칙 데이터**로 정의하고 JS/Java 양쪽 파서 라이브러리로 실행 | 프론트 로직을 전면 제거하고 입력 변경 시 **실시간 계산 API** 호출 | 단일 `.js` 수식 파일을 작성하고 백엔드에서 **GraalVM 내장 JS 엔진**으로 구동 |
| **SSOT 위치** | 중앙 데이터베이스 / `rule.json` | 백엔드 Java Service 계층 단일 지점 | 단일 공통 `calc.js` 스크립트 |
| **장점** | • 수식 변경 시 서버/클라이언트 재배포 불필요<br>• 네트워크 지연 없이 즉각 실행 | • 프론트엔드 계산 로직 0% (구현 단순)<br>• 100% 동일한 연산 엔진 보장 | • JS 문법 그대로 활용 가능<br>• 네트워크 I/O 오버헤드 없음 |
| **단점/한계** | • 복잡한 중첩 수식/조건문 작성 시 JSON 가독성 저하<br>• 프론트/백 양측에 파서 라이브러리 종속 | • 키 입력 시 네트워크 지연(0.x초) 발생 (UX)<br>• 대량 입력 시 백엔드 요청 트래픽 증가 | • GraalVM 설정 복잡도 및 메모리 오버헤드<br>• 단순 산술 연산 대비 과도한 아키텍처 복잡성 |


## 3. 근본 원인 (Root Cause)

> **"비즈니스 규칙이 코드로 파편화되어 단일 진실 공급원(SSOT)이 부재했던 구조적 결함"**

1. **언어 파편화 (JavaScript vs Java):** 프론트엔드와 백엔드의 런타임 언어가 달라 비즈니스 로직을 물리적으로 공유하지 못하고 '복사-붙여넣기식 구현'에 의존함.
2. **동기화 실패:** 수식 변경 라이프사이클이 프론트와 백엔드 간에 분리되어 있어, 수정 누락이나 부동소수점 연산 오차 등의 잠재적 버그가 지속적으로 양산됨.


## 4. 해결 방법: 도메인 특성에 맞춘 실전 적용

단순한 오버엔지니어링(GraalVM 도입 등)을 피하고, 실무 환경(폐쇄망/내부망 환경 및 빠른 개발 생산성)에 맞춰 **실시간 API 위임 방식(API Driven)**과 **검증 파이프라인 단일화**를 적용함.

### ① Frontend: React Query + Debounce 기반 서버 계산 위임

- 사용자가 인구수/면적 수치를 빠르게 연속 입력할 때 불필요한 서버 호출 폭증을 막기 위해 **Debounce(300~500ms)**를 적용.
- 프론트의 자체 수식 계산 코드를 전면 제거하고, 디바운스된 최종 입력값으로 백엔드 단일 계산 API를 호출하여 화면 상태를 갱신하도록 통일.

### ② Backend: 비즈니스 계산 및 최종 검증 엔진 일원화

- 실시간 계산용 엔드포인트와 최종 저장(`submit`) 트랜잭션 내부에서 **동일한 Service Layer 검증/연산 메서드**를 재사용하도록 구조를 표준화.

```java
@Service
@RequiredArgsConstructor
public class SewerageSupplyRateService {

    // 실시간 화면 계산 및 최종 저장 검증 시 동일하게 호출되는 단일 연산 메서드
    public SewerageCalculationResult calculateMetrics(SewerageMetricsRequestDto dto) {
        // 합계 및 보급률 단일 계산 로직 (SSOT)
        long totalPopulation = calculateTotalPopulation(dto);
        double supplyRate = calculateSupplyRate(totalPopulation, dto.getAdministrativeArea());
        
        return SewerageCalculationResult.builder()
                .popltnSm(totalPopulation)
                .supplyRate(supplyRate)
                .build();
    }
}
```

## 5. 결과

1. **정합성 오류 0건 달성:** 프론트와 백엔드의 계산 공식 차이로 인해 발생하던 런타임 검증 에러 및 저장 거부 현상 완전 제거.
2. **유지보수 생산성 향상:** 통계/검증 산식이 변경되어도 백엔드 Service 로직 1곳만 수정하면 화면과 검증 파이프라인 전체에 즉시 동기화됨.
3. **불필요한 기술 부채 방지:** 중복 코드 작성에 들던 개발 공수를 제거하고, 단일 테스트 코드로 연산 무결성을 검증할 수 있는 환경 구축.

## 6. 배운점

- **UI 편의성을 이유로 비즈니스 규칙을 복제하지 않는다:** 화면 피드백을 빠르게 주겠다는 이유로 프론트엔드에 핵심 수식을 복사해 넣는 순간 시스템의 무결성은 깨지기 시작한다.
- **SSOT(Single Source of Truth)의 절대적 가치:** 비즈니스 규칙은 언제나 '단 하나의 출처'에서 관리되고 실행되어야 데이터 정합성을 담보할 수 있다.
- **상황에 맞는 최적의 설계 선택:** GraalVM이나 JSON Logic 같은 화려한 기술이 항상 정답은 아니다. 팀의 인프라와 유지보수 비용을 고려할 때 **Debounce + API 위임**처럼 단순하면서도 확실한 패턴이 정답이 될 수 있다.
